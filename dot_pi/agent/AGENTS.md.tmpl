{{ template "agent-instructions" . }}

## Documentation Lookup
Use Context7 tools for up-to-date docs whenever the user asks about a library, framework, SDK, API, CLI tool, or cloud service — even well-known ones.
1. `context7_resolve_library_id` to find the library
2. `context7_get_library_docs` with the library ID
